<h2>Add a review to the restaurant</h2>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit a review"%>
<% end %>

<%= link_to "Back to all restaurant", restaurants_path %>
